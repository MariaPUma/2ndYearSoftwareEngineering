import java.util.ArrayList;
import java.util.List;

public class Notas {
	/*
	 * Tenemos que tener en cuenta que las posiciones que añadamos a la lista
	 * no pueden estar contenidas en la llista del recorrido 
	 * Aparte de controlar que este dentro del laberinto y no sea un muro
	 * 
	 * El objetivo es minimizar el camino 
	 * 
	 * 
	 * Lanza el vuelta atrás para encontrar el camino
	 * public List<Posicion> encontrarCamino() {
		List<Posicion> lista = new ArrayList<Posicion>();
		lista.add(entrada);
		return (encontrarCamino(lista)) ? lista : null;
	}


	 */

}
