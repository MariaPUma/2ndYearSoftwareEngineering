#include <stdio.h>
/*
Las listas enlazadas están formadas por un puntero externo que apunta a un nodo. El nodo está formado con una
parte de datos y un puntero que apunta al siguiente nodo.
El final de la lista se caracteriza por el puntero que almacena, este tendrá el valor null.

Trabajar con las listas:
Hay configurar los módulos (Está en el campus virtual)
en visual code se crearán tres ficheros: principal.c , MLista.h (módulo), MLista.c

Para poder compilar hay que añadir a la carpeta de ejercicios el .vscode para windows + el esqueleto de Milista
Para construir una lista enlazada:
typedef struct TNodo * TLista;
struct TNodo{
    int valor;
    TLista sig;
}


08/03/24
En el caso de que queramos insertar un elemento de forma ordenada y sin repeticiones:


NULL es un indicador para punteros que indica que el puntero no está apuntando a nada
*/
int main (void){

}